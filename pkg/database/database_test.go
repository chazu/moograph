package database

import (
	"fmt"
	"os"
	"strings"
	"testing"
)

func TestRootObject(t *testing.T) {
	// b, err := os.ReadFile("./../testdata/Test.db")
	// if err != nil {
	// 	fmt.Errorf("Error opening database file: %v", err.Error())
	// }

	// lines := strings.Split(string(b), "\n")
	// db := NewDatabase(lines)

	// db.Parse()
}
